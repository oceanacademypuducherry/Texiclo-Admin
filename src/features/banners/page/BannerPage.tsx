import { PlaceHolder } from "../../shared";
import { Banner } from "../component";
import { BannerData } from "../data/bannerdata";

export const BannerPage = () => {
  return (
    <PlaceHolder>
      {/* <div className="p-6">
        {BannerData.map((banner) => (
          <Banner key={banner.id} image={banner.image} title={banner.title} />
        ))}
      </div> */}
      <div>bannerpage</div>
    </PlaceHolder>
  );
};
